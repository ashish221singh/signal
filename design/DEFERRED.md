# DEFERRED — previous Signal design system, prototype & logo

**As of 2026-08-03**, the following are **deferred** (kept for reference, no longer the
active direction) in favour of a new brand + product design under
[`design/final-version/`](final-version/):

- `design/design-system.md`, `design/signal-design-system.html`, `design/tokens.css`
- `design/logo/` (all explorations + `final/` marks — the blocky "S" lettermark)
- `prototypes/signal-console-prototype.html`

**What carries over:** the **colour scheme only** — orange `#F78200` (accent),
blue `#0094DD`, warm ink `#191815`, off-white `#FCFCFB`, and the warm gray ramp.

**What changes:** logo/mark, overall visual approach, motion language. New direction is
**AI-native, minimal, "Signal alive"** with a **solid-monochrome mark** and futuristic,
smooth motion. See `design/final-version/`.
