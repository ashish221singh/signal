# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# generated token/font bundle (reproducible from tokens.css + fonts via scripts/generate.mjs)
packages/tokens/src/generated.ts

# environment — never commit real env values
.env
.env.*
!.env.example

# test artifacts
coverage/

# git worktrees (isolated workspaces)
.worktrees/

# OS / editor noise
.DS_Store
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# Android SDK build artifacts
sdk-android/.gradle/
sdk-android/build/
sdk-android/**/build/
.cxx/
local.properties

# uncommitted design-lab scratch (not part of the build)
apps/studio/
