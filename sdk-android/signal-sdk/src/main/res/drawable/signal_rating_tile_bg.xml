<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded white background for emoji tiles (radius ~15dp). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/signal_bg" />
    <corners android:radius="@dimen/signal_radius_emoji" />
    <stroke
        android:width="1dp"
        android:color="@color/signal_line_subtle" />
</shape>
