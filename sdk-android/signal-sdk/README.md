# Signal SDK (module)

The integration + testing guide (setup, `Signal.init`, hooks, behaviour, build,
local backend, and the manual QA checklist) lives in the SDK root README:
[`../README.md`](../README.md).
