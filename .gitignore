# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# environment — never commit real env values
.env
.env.*
!.env.example

# test artifacts
coverage/

# OS / editor noise
.DS_Store
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# frontend prototype — kept local until explicitly ready to publish
prototypes/
