<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded white background for effort-scale segments (radius ~12dp). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/signal_bg" />
    <corners android:radius="@dimen/signal_radius_segment" />
    <stroke
        android:width="1dp"
        android:color="@color/signal_line" />
</shape>
